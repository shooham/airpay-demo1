{
  "name": "airpay-integration-dependencies",
  "description": "Required dependencies for AirPay payment gateway integration",
  "dependencies": {
    "axios": "^1.6.0",
    "crypto": "built-in",
    "mongoose": "^7.0.0",
    "express": "^4.18.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "installation": {
    "command": "npm install axios mongoose express dotenv",
    "note": "crypto is a built-in Node.js module, no installation required"
  },
  "requirements": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0",
    "mongodb": ">=4.0.0"
  }
}